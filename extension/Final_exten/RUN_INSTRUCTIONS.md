Parent password:

1. Open Attenova and click `Parent Mode`.
2. The first time, create and confirm the parent password.
3. After that, `Parent Mode` requires the password each time the popup is opened.
4. `Child Mode` stays available without a password.